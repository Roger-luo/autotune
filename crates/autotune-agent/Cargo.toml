[package]
name = "autotune-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
quick-xml = "0.36"
autotune-config = { path = "../autotune-config" }

[dev-dependencies]
tempfile = "3"
