[package]
name = "autotune-config"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1"
thiserror = "2"
globset = "0.4"
dirs = "6"

[dev-dependencies]
tempfile = "3"
