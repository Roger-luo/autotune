---
title: autotune-benchmark
description: API reference for the autotune-benchmark crate, generated from rustdoc.
section: API Reference
sidebarLabel: autotune-benchmark
order: 4
---

<!-- GENERATED by scripts/gen-api.mjs from rustdoc JSON. Do not edit by hand; run `pnpm api:gen`. -->

> Generated from rustdoc JSON. Run `pnpm api:gen` to refresh after changing the crate.

## Structs

### JudgeContext

`autotune_benchmark::JudgeContext`

```rust
struct JudgeContext<'a> { /* fields */ }
```

Carries the judge agent and its config into the measuring phase.

#### Fields

- `agent: &'a dyn autotune_agent::Agent`
- `agent_config: autotune_agent::AgentConfig`
- `make_stream: Option<Box<JudgeStreamFactory>>` — Optional factory called once per judge invocation. Receives a status string and returns an event handler plus a finish closure to call after the agent returns (clears ephemeral status lines, flushes buffered text).

### MeasureReport

`autotune_benchmark::MeasureReport`

```rust
struct MeasureReport { /* fields */ }
```

Result of running a single measure: the raw stdout/stderr plus the
extracted metrics. Raw output is retained so callers can persist it for
later inspection (e.g. by a research agent looking for context beyond the
summary metrics).

#### Fields

- `name: String`
- `stdout: String`
- `stderr: String`
- `metrics: autotune_adaptor::Metrics`

**Implements:** `Clone`, `Debug`

## Enums

### MeasureError

`autotune_benchmark::MeasureError`

```rust
enum MeasureError
```

Errors returned by measure execution and metric extraction.

#### Variants

- `CommandFailed { name, code, stderr }`
- `Io { name, source }`
- `TimedOut { name, timeout }`
- `Extraction { name, source }`

**Implements:** `Debug`, `Display`, `Error`

## Functions

### build_adaptor

`autotune_benchmark::build_adaptor`

```rust
fn build_adaptor(config: &autotune_config::AdaptorConfig, working_dir: &std::path::Path) -> Box<dyn MetricAdaptor>
```

Build a MetricAdaptor from config.

### run_all_measures

`autotune_benchmark::run_all_measures`

```rust
fn run_all_measures(configs: &[autotune_config::MeasureConfig], working_dir: &std::path::Path, approach_name: &str, iteration: u32, judge_ctx: Option<&JudgeContext<'_>>) -> Result<autotune_adaptor::Metrics, MeasureError>
```

Run all configured measures and merge their metrics.

### run_all_measures_with_output

`autotune_benchmark::run_all_measures_with_output`

```rust
fn run_all_measures_with_output(configs: &[autotune_config::MeasureConfig], working_dir: &std::path::Path, approach_name: &str, iteration: u32, judge_ctx: Option<&JudgeContext<'_>>) -> Result<(autotune_adaptor::Metrics, Vec<MeasureReport>), MeasureError>
```

Run all configured measures, returning the merged metrics and the per-measure
raw output reports (in the order the measures were configured).

### run_judge_measure

`autotune_benchmark::run_judge_measure`

```rust
fn run_judge_measure(config: &autotune_config::MeasureConfig, working_dir: &std::path::Path, approach_name: &str, iteration: u32, ctx: &JudgeContext<'_>) -> Result<MeasureReport, MeasureError>
```

Run a judge measure: optionally run a command, build a subject, call the
batch judge, and return one metric per rubric ID.

### run_measure

`autotune_benchmark::run_measure`

```rust
fn run_measure(config: &autotune_config::MeasureConfig, working_dir: &std::path::Path) -> Result<autotune_adaptor::Metrics, MeasureError>
```

Run a single measure command and extract metrics.

### run_measure_with_output

`autotune_benchmark::run_measure_with_output`

```rust
fn run_measure_with_output(config: &autotune_config::MeasureConfig, working_dir: &std::path::Path) -> Result<MeasureReport, MeasureError>
```

Run a single measure command, returning the extracted metrics along with
the raw stdout/stderr captured during the run.

## Type aliases

### JudgeStreamFactory

`autotune_benchmark::JudgeStreamFactory`

```rust
type JudgeStreamFactory = dyn Fn(&str) -> (autotune_agent::EventHandler, Box<dyn FnOnce()>) + Send + Sync;
```

Factory that creates a streaming handler for one judge invocation.
Receives a status string; returns an event handler and a finish closure.

