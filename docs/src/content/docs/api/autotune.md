---
title: autotune
description: API reference for the autotune crate, generated from rustdoc.
section: API Reference
sidebarLabel: autotune
order: 1
---

<!-- GENERATED by scripts/gen-api.mjs from rustdoc JSON. Do not edit by hand; run `pnpm api:gen`. -->

> Generated from rustdoc JSON. Run `pnpm api:gen` to refresh after changing the crate.

## Structs

### RunContext

`autotune::machine::RunContext`

```rust
struct RunContext<'a> { /* fields */ }
```

Bundles optional per-run context that doesn't fit the core state machine
parameters — the tool approver for human-in-the-loop confirmations, and
the judge context for LLM-based measure adaptors.

#### Fields

- `approver: Option<&'a dyn autotune_plan::ToolApprover>`
- `judge_ctx: Option<&'a autotune_benchmark::JudgeContext<'a>>`

### Stream

`autotune::stream_ui::Stream`

```rust
struct Stream { /* fields */ }
```

A streaming UI session that forwards agent events to stderr with buffered
markdown rendering.

Create one per agent invocation. Call `handler()` to get an `EventHandler`
(you can call it multiple times; all handlers share the same state), pass
it to the agent, then call `finish()` after the agent returns to flush any
trailing buffered markdown and clear the ephemeral status line.

_All fields are private._

#### Methods

```rust
fn research(status: &str) -> Self
```

Stream for the research agent. Buffers markdown and suppresses the
`<plan>` / `<request-tool>` XML payload once it begins.

```rust
fn implementation(status: &str) -> Self
```

Stream for the implementation agent. Buffers markdown; there is no
protocol payload to suppress.

```rust
fn judge(status: &str) -> Self
```

Stream for the judge agent. Buffers markdown; no protocol payload to suppress.

```rust
fn handler(&self) -> EventHandler
```

Build an `EventHandler` for the underlying agent. Safe to call multiple
times — each returned handler shares the same buffering state.

```rust
fn finish(&self)
```

Flush any buffered markdown, render a final newline, and clear the
ephemeral status line. Call after the agent returns.

### TerminalToolApprover

`autotune::stream_ui::TerminalToolApprover`

```rust
struct TerminalToolApprover { /* fields */ }
```

Interactive approval for runtime tool requests from the research agent.

Each request shows the tool + scope + reason and asks the user to allow
(for the rest of the task run) or deny. Hard-denied tools are rejected
upstream before reaching this type.

**Implements:** `ToolApprover`

## Enums

### AgentRole

`autotune::agent_factory::AgentRole`

```rust
enum AgentRole
```


#### Variants

- `Research`
- `Implementation`
- `Init`
- `Judge`

**Implements:** `Clone`, `Copy`, `Debug`, `Eq`, `PartialEq`

## Functions

### build_agent_for_backend

`autotune::agent_factory::build_agent_for_backend`

```rust
fn build_agent_for_backend(backend: &str) -> anyhow::Result<Box<dyn Agent>>
```

### clear_status

`autotune::stream_ui::clear_status`

```rust
fn clear_status()
```

Clear the current terminal line. Used by the tool-approval prompt to wipe
any leftover ephemeral status before showing an interactive question.

### prepare_resume

`autotune::resume::prepare_resume`

```rust
fn prepare_resume(store: &autotune_state::TaskStore, repo_root: &std::path::Path) -> anyhow::Result<autotune_state::TaskState>
```

Prepare a task for resumption by recovering from any incomplete phase.

Returns the state ready to be fed into `run_task`.

### render_hypothesis

`autotune::stream_ui::render_hypothesis`

```rust
fn render_hypothesis(iteration: usize, hypothesis: &autotune_plan::Hypothesis)
```

Render the proposed hypothesis to stderr so the user can see what the
research agent chose before the implementation phase runs.

### resolve_backend_name

`autotune::agent_factory::resolve_backend_name`

```rust
fn resolve_backend_name(config: &autotune_config::AgentConfig, role: AgentRole) -> Option<&str>
```

### run_single_phase

`autotune::machine::run_single_phase`

```rust
fn run_single_phase(config: &autotune_config::AutotuneConfig, agent: &dyn Agent, scorer: &dyn ScoreCalculator, repo_root: &std::path::Path, store: &autotune_state::TaskStore, state: &mut autotune_state::TaskState, ctx: &RunContext<'_>) -> anyhow::Result<bool>
```

Execute exactly one phase transition, returning the expected phase that was
executed. Returns `Ok(true)` if the task has reached the Done phase.

### run_task

`autotune::machine::run_task`

```rust
fn run_task(config: &autotune_config::AutotuneConfig, agent: &dyn Agent, scorer: &dyn ScoreCalculator, repo_root: &std::path::Path, store: &autotune_state::TaskStore, shutdown: &ShutdownFlag, ctx: &RunContext<'_>) -> anyhow::Result<()>
```

## Type aliases

### ShutdownFlag

`autotune::machine::ShutdownFlag`

```rust
type ShutdownFlag = std::sync::atomic::AtomicBool;
```

