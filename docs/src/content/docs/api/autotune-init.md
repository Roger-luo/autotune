---
title: autotune-init
description: API reference for the autotune-init crate, generated from rustdoc.
section: API Reference
sidebarLabel: autotune-init
order: 8
---

<!-- GENERATED by scripts/gen-api.mjs from rustdoc JSON. Do not edit by hand; run `pnpm api:gen`. -->

> Generated from rustdoc JSON. Run `pnpm api:gen` to refresh after changing the crate.

## Structs

### InitResult

`autotune_init::InitResult`

```rust
struct InitResult { /* fields */ }
```

Result of a successful init: the config and optional baseline metrics
(present when a config validator was provided and succeeded).

#### Fields

- `config: autotune_config::AutotuneConfig`
- `baseline_metrics: Option<std::collections::HashMap<String, f64>>`

### MockInput

`autotune_init::input::MockInput`

```rust
struct MockInput { /* fields */ }
```

Simple input for testing. Always returns the same response.

_All fields are private._

#### Methods

```rust
fn new(response: &str) -> Self
```


**Implements:** `UserInput`

### TerminalInput

`autotune_init::input::TerminalInput`

```rust
struct TerminalInput { /* fields */ }
```

Interactive terminal input.
Uses dialoguer arrow-key selection when stdin is a TTY.
Falls back to line-based input when stdin is piped.

When `history_file` is set, `prompt_text` saves and restores history across
sessions using rustyline (arrow-key navigation + Ctrl-R reverse search).

_All fields are private._

#### Methods

```rust
fn new() -> Self
```

```rust
fn with_history(history_file: PathBuf) -> Self
```


**Implements:** `Default`, `UserInput`

## Enums

### InitError

`autotune_init::InitError`

```rust
enum InitError
```


#### Variants

- `Agent { source }`
- `Config { source }`
- `UserAborted`
- `ProtocolFailure { message }`
- `Io { source }`

**Implements:** `Debug`, `Display`, `Error`, `From<AgentError>`, `From<ConfigError>`, `From<Error>`

## Traits

### UserInput

`autotune_init::input::UserInput`

```rust
trait UserInput
```

Trait for user interaction during the init conversation.
Implementations handle text prompts, option selection, and approval.

#### Required / provided methods

```rust
fn prompt_text(&self, message: &str) -> Result<String, io::Error>
```

Show a message and read a free-form text response.

```rust
fn prompt_select(&self, question: &str, options: &[QuestionOption], allow_free_response: bool) -> Result<String, io::Error>
```

Show a question with selectable options. Returns the selected option's key
or a free-text response.

```rust
fn prompt_approve(&self, message: &str) -> Result<bool, io::Error>
```

Show a yes/no approval prompt. Returns true if approved.

## Functions

### build_init_prompt

`autotune_init::build_init_prompt`

```rust
fn build_init_prompt(repo_root: &std::path::Path) -> String
```

Build the system prompt for the init agent.

Includes the XML protocol schema, config section descriptions, and
instructions for exploring the codebase before proposing config.

### run_init

`autotune_init::run_init`

```rust
fn run_init(agent: &dyn Agent, global_config: &autotune_config::global::GlobalConfig, repo_root: &std::path::Path, user_input: &dyn UserInput, config_validator: Option<&ConfigValidator<'_>>) -> Result<InitResult, InitError>
```

Run the agent-assisted init conversation.

`user_input` handles all user interaction (text prompts, option selection, approval).
Use `TerminalInput` for real CLI sessions or `MockInput` for testing.

If `config_validator` is provided, it is called after user approval to validate the
config (e.g., by running a trial measure). On failure, the user is asked whether
to let the agent revise the config.

## Type aliases

### ConfigValidator

`autotune_init::ConfigValidator`

```rust
type ConfigValidator<'a> = dyn Fn(&autotune_config::AutotuneConfig) -> Result<std::collections::HashMap<String, f64>, String> + 'a;
```

Callback to validate a proposed config before finalizing.

Called after the user approves the assembled config. Typically runs the
measure commands and tries metric extraction. Returns extracted metrics
on success, or a detailed error string (including measure output) on failure.

