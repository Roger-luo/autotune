---
title: autotune-agent
description: API reference for the autotune-agent crate, generated from rustdoc.
section: API Reference
sidebarLabel: autotune-agent
order: 3
---

<!-- GENERATED by scripts/gen-api.mjs from rustdoc JSON. Do not edit by hand; run `pnpm api:gen`. -->

> Generated from rustdoc JSON. Run `pnpm api:gen` to refresh after changing the crate.

## Structs

### AgentConfig

`autotune_agent::AgentConfig`

```rust
struct AgentConfig { /* fields */ }
```


#### Fields

- `prompt: String`
- `allowed_tools: Vec<ToolPermission>`
- `working_directory: std::path::PathBuf`
- `model: Option<String>`
- `max_turns: Option<u64>`
- `reasoning_effort: Option<String>`

**Implements:** `Clone`, `Debug`

### AgentConfigWithEvents

`autotune_agent::AgentConfigWithEvents`

```rust
struct AgentConfigWithEvents { /* fields */ }
```

Extended config with optional event streaming. Used internally
by agent implementations — callers set the handler via `with_event_handler`.

#### Fields

- `config: AgentConfig`
- `event_handler: Option<EventHandler>`

#### Methods

```rust
fn new(config: AgentConfig) -> Self
```

```rust
fn with_event_handler(self, handler: EventHandler) -> Self
```

### AgentResponse

`autotune_agent::AgentResponse`

```rust
struct AgentResponse { /* fields */ }
```


#### Fields

- `text: String`
- `session_id: String`

**Implements:** `Clone`, `Debug`

### AgentSession

`autotune_agent::AgentSession`

```rust
struct AgentSession { /* fields */ }
```


#### Fields

- `session_id: String`
- `backend: String`

**Implements:** `Clone`, `Debug`

### ClaudeAgent

`autotune_agent::claude::ClaudeAgent`

```rust
struct ClaudeAgent { /* fields */ }
```


_All fields are private._

#### Methods

```rust
fn new() -> Self
```

```rust
fn with_command(command: PathBuf) -> Self
```

Create with a custom binary path (useful for testing with mock binaries).


**Implements:** `Agent`, `Default`

### CodexAgent

`autotune_agent::codex::CodexAgent`

```rust
struct CodexAgent { /* fields */ }
```


_All fields are private._

#### Methods

```rust
fn new() -> Self
```

```rust
fn with_command(command: PathBuf) -> Self
```

```rust
fn with_command_and_codex_home(command: PathBuf, codex_home: Option<PathBuf>) -> Self
```


**Implements:** `Agent`, `Default`

### Guard

`autotune_agent::terminal::Guard`

```rust
struct Guard(_);
```

RAII guard that calls [`restore`] on drop.

Hold one in scope for the duration of any code that may alter terminal
state — spawning the Claude CLI, a `dialoguer` prompt, or a crossterm
raw-mode block. The guard fires on every way the scope can exit: normal
return, `?` propagation, and unwinding panics.

```ignore
let _guard = autotune_agent::terminal::Guard::new();
run_some_interactive_subprocess()?;
// terminal is restored here when _guard drops
```

#### Methods

```rust
fn new() -> Self
```


**Implements:** `Default`, `Drop`

### QuestionOption

`autotune_agent::protocol::QuestionOption`

```rust
struct QuestionOption { /* fields */ }
```

A question option rendered as a selection menu item.

#### Fields

- `key: String`
- `label: String`
- `description: Option<String>`

**Implements:** `Clone`, `Debug`, `Eq`, `PartialEq`

### RubricProposal

`autotune_agent::protocol::RubricProposal`

```rust
struct RubricProposal { /* fields */ }
```

A rubric proposed by the agent during the judge measure interview.

#### Fields

- `id: String`
- `title: String`
- `instruction: String`
- `score_min: i32`
- `score_max: i32`

**Implements:** `Clone`, `Debug`, `PartialEq`

### TagMatch

`autotune_agent::protocol::TagMatch`

```rust
struct TagMatch<'a> { /* fields */ }
```

A substring match found by [`lenient_find_all`].

#### Fields

- `start: usize` — Byte offset of the opening `<tag>` in the source string.
- `inner: &'a str` — Content between `<tag>` and `</tag>`, not including the tags themselves.
- `outer: &'a str` — Full `<tag>…</tag>` substring including both tags.

**Implements:** `Clone`, `Copy`, `Debug`

### ToolRequest

`autotune_agent::protocol::ToolRequest`

```rust
struct ToolRequest { /* fields */ }
```

A request from an agent to be granted additional tool access for its session.
Emitted as a `<request-tool>` XML fragment in the agent response.

#### Fields

- `tool: String` — Tool name, e.g. "Bash", "WebFetch".
- `scope: Option<String>` — Optional scope (e.g. "cargo tree:*" for Bash). When None, unscoped Allow.
- `reason: String` — Why the agent wants this tool — shown verbatim to the user.

**Implements:** `Clone`, `Debug`, `Eq`, `PartialEq`

## Enums

### AgentError

`autotune_agent::AgentError`

```rust
enum AgentError
```


#### Variants

- `CommandFailed { message }`
- `ParseFailed { message }`
- `Timeout { seconds }`
- `Interrupted`
- `Io { source }`

**Implements:** `Debug`, `Display`, `Error`, `From<Error>`

### AgentEvent

`autotune_agent::AgentEvent`

```rust
enum AgentEvent
```

Events emitted by the agent during execution.

#### Variants

- `ToolUse { tool, input_summary }` — Agent is using a tool (e.g., Read, Glob, Grep).
- `Text(String)` — Agent produced intermediate text output.

**Implements:** `Clone`, `Debug`

### AgentFragment

`autotune_agent::protocol::AgentFragment`

```rust
enum AgentFragment
```

One top-level fragment emitted by the agent during a single turn.
A turn may produce zero or more of these.

#### Variants

- `Message(String)` — Free-form prose to show the user; user types a reply.
- `Question { text, options, allow_free_response }` — Structured question with selectable options.
- `Task(autotune_config::TaskConfig)` — A proposed `[task]` section.
- `Paths(autotune_config::PathsConfig)` — A proposed `[paths]` section.
- `Test(autotune_config::TestConfig)` — A proposed `[[test]]` entry.
- `Measure(autotune_config::MeasureConfig)` — A proposed `[[measure]]` entry.
- `Score(autotune_config::ScoreConfig)` — A proposed `[score]` section.
- `Agent(Box<autotune_config::AgentConfig>)` — A proposed `[agent]` section.
- `Rubric(RubricProposal)` — A proposed rubric for the pending judge measure, shown to the user for approval.
- `RubricsDone` — Signals the agent has finished proposing rubrics for the current judge measure.

**Implements:** `Clone`, `Debug`

### ToolPermission

`autotune_agent::ToolPermission`

```rust
enum ToolPermission
```


#### Variants

- `Allow(String)`
- `AllowScoped(String, String)`
- `Deny(String)`

**Implements:** `Clone`, `Debug`

### TraceInitError

`autotune_agent::trace::TraceInitError`

```rust
enum TraceInitError
```

Error returned when `init()` cannot set up the trace file.

#### Variants

- `AlreadyExists { path }`
- `Create { path, source }`
- `AlreadyInitialized`

**Implements:** `Debug`, `Display`, `Error`

## Traits

### Agent

`autotune_agent::Agent`

```rust
trait Agent
```


#### Required / provided methods

```rust
fn spawn(&self, config: &AgentConfig) -> Result<AgentResponse, AgentError>
```

```rust
fn send(&self, session: &AgentSession, message: &str) -> Result<AgentResponse, AgentError>
```

```rust
fn spawn_streaming(&self, config_with_events: AgentConfigWithEvents) -> Result<AgentResponse, AgentError>
```

Spawn with event streaming. Default falls back to non-streaming spawn.

```rust
fn send_streaming(&self, session: &AgentSession, message: &str, event_handler: Option<&EventHandler>) -> Result<AgentResponse, AgentError>
```

Send with event streaming. Default falls back to non-streaming send.

```rust
fn backend_name(&self) -> &str
```

```rust
fn handover_command(&self, session: &AgentSession) -> String
```

```rust
fn hydrate_session(&self, session: &AgentSession, config: &AgentConfig) -> Result<(), AgentError>
```

Seed backend-specific session context for a resumed session when the
process no longer has the original in-memory state from `spawn()`.

Backends that require remembered working directory / model / permission
config across `send()` calls should override this. Backends that can
resume from session id alone may ignore it.

```rust
fn grant_session_permission(&self, session: &AgentSession, permission: ToolPermission) -> Result<(), AgentError>
```

Add a tool permission to an existing session so subsequent `send_streaming`
calls will include it in the allowed tools list. Used to approve runtime
tool requests from an agent. Default impl errors — backends that keep
per-session context should override.

## Functions

### init

`autotune_agent::trace::init`

```rust
fn init() -> Result<(), TraceInitError>
```

Explicitly initialize tracing from `AUTOTUNE_TRACE_FILE`.

Must be called once, before any `record()` call. Returns an error if:
- the file already exists (`AlreadyExists`) — delete it or use a new path,
- the path is unwritable / the parent directory is missing (`Create`),
- `init()` was called after `record()` already lazily initialized the
  writer (`AlreadyInitialized`).

When `AUTOTUNE_TRACE_FILE` is not set, `init()` is a no-op and returns `Ok`.

### install_panic_hook

`autotune_agent::terminal::install_panic_hook`

```rust
fn install_panic_hook()
```

Install a panic hook that calls [`restore`] before delegating to the
previous hook. Idempotent: safe to call multiple times.

Call this once early in `main()`. Combined with per-operation [`Guard`]
instances, the terminal is restored on every exit path *except* direct
`std::process::exit` — which neither runs Drop nor triggers the panic
hook. Signal handlers should call [`restore`] explicitly before exiting.

### is_enabled

`autotune_agent::trace::is_enabled`

```rust
fn is_enabled() -> bool
```

True when `AUTOTUNE_TRACE_FILE` was set and the file opened successfully.
Callers can use this to skip building an expensive payload that would be
discarded anyway.

### lenient_find_all

`autotune_agent::protocol::lenient_find_all`

```rust
fn lenient_find_all<'a>(text: &'a str, tag: &str) -> Vec<TagMatch<'a>>
```

Find all `<tag>…</tag>` occurrences by literal substring matching.

Returns one [`TagMatch`] per pair found, in document order. Does not attempt
XML parsing — `inner` may contain any characters, including unescaped `<`,
`>`, and `&`. Unterminated opens (no matching `</tag>`) are silently
skipped rather than treated as errors.

Only matches literal `<tag>` opens; attribute-bearing opens like
`<tag foo="bar">` are not recognized (none of our protocol tags use
attributes).

### parse_agent_response

`autotune_agent::protocol::parse_agent_response`

```rust
fn parse_agent_response(response: &str) -> Result<Vec<AgentFragment>, AgentError>
```

Parse an agent response into zero or more fragments.

Uses lenient substring extraction at the top level: each known tag
(`<message>`, `<question>`, `<task>`, `<paths>`, `<test>`, `<measure>`,
`<score>`, `<agent>`) is found by literal string matching. The strict XML
parser runs only on each matched fragment's content, so garbage prose
between fragments (unescaped `<`, `&`, Rust generics, markdown tables)
is harmlessly ignored.

Fragments are returned in document order (sorted by byte offset).
Unknown tags and unmatched text are silently skipped.

### parse_tool_requests

`autotune_agent::protocol::parse_tool_requests`

```rust
fn parse_tool_requests(response: &str) -> Result<Vec<ToolRequest>, AgentError>
```

Parse any `<request-tool>` top-level fragments in an agent response.

Only `<request-tool>` blocks matter here — everything else is prose, plans,
or fragments handled by a different parser. In practice the rest of the
response is full of things that aren't well-formed XML: agents routinely
embed Rust type signatures (`&Value`, `Vec<T>`), code snippets with `<` and
`&`, and markdown tables. Walking the whole document with strict quick-xml
blows up on the first such occurrence and takes down the tune loop.

So we do a lenient string scan: find each literal `<request-tool>` …
`</request-tool>` pair and run the strict parser only on that substring.
Whatever sits between pairs is ignored verbatim, regardless of whether it
parses as XML.

Matches that sit inside another known wrapper fragment (see
[`WRAPPER_TAGS`]) are skipped: the research agent legitimately writes
literal `<request-tool>` examples inside a `<plan>`'s `<hypothesis>` when
describing tests, and those must not be parsed as real requests.

Attribute-bearing opens (`<request-tool foo="bar">`) are not supported —
the schema doesn't use attributes on this tag, and accepting them here
would force us back onto the strict walker.

### record

`autotune_agent::trace::record`

```rust
fn record(category: &str, payload: serde_json::Value)
```

Append one trace record. Safe to call from any thread; no-op when tracing
is disabled. Errors (poisoned mutex, IO failure) are swallowed — a broken
trace must not take down the tune loop.

### restore

`autotune_agent::terminal::restore`

```rust
fn restore()
```

Write terminal-restore CSI sequences to stderr, if stderr is a TTY.

No-op in non-interactive contexts (piped, redirected, test runner).

## Macros

### aeprintln

`autotune_agent::aeprintln`

```rust
macro_rules! aeprintln {
    ($($arg:tt)*) => { ... };
}
```

Like [`eprintln!`], but renders the line in Autotune's accent color when
stderr is a TTY. Use for `[autotune]`-tagged status lines on stderr.

### aprintln

`autotune_agent::aprintln`

```rust
macro_rules! aprintln {
    ($($arg:tt)*) => { ... };
}
```

Like [`println!`], but renders the line in Autotune's accent color when
stdout is a TTY. Use for `[autotune]`-tagged status lines on stdout.

## Type aliases

### EventHandler

`autotune_agent::EventHandler`

```rust
type EventHandler = Box<dyn Fn(AgentEvent) + Send + Sync>;
```

Callback for receiving agent events during execution.
The agent calls this for each streaming event. Implementations
should be cheap (e.g., print to stderr) since they block the read loop.

