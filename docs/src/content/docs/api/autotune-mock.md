---
title: autotune-mock
description: API reference for the autotune-mock crate, generated from rustdoc.
section: API Reference
sidebarLabel: autotune-mock
order: 10
---

<!-- GENERATED by scripts/gen-api.mjs from rustdoc JSON. Do not edit by hand; run `pnpm api:gen`. -->

> Generated from rustdoc JSON. Run `pnpm api:gen` to refresh after changing the crate.

## Structs

### MockAgent

`autotune_mock::MockAgent`

```rust
struct MockAgent { /* fields */ }
```

A configurable mock LLM agent for testing the autotune pipeline
end-to-end without real LLM calls.

_All fields are private._

#### Methods

```rust
fn builder() -> MockAgentBuilder
```

Create a new builder for configuring a `MockAgent`.

```rust
fn spawn_count(&self) -> usize
```

Number of times `spawn()` has been called.

```rust
fn send_count(&self) -> usize
```

Number of times `send()` has been called.

```rust
fn last_spawn_config(&self) -> Option<AgentConfig>
```

Clone of the last `AgentConfig` passed to `spawn()`.

```rust
fn last_send_message(&self) -> Option<String>
```

Last message passed to `send()`.

```rust
fn spawn_configs(&self) -> Vec<AgentConfig>
```

All configs passed to `spawn()`, in order.

```rust
fn send_messages(&self) -> Vec<String>
```

All messages passed to `send()`, in order.

```rust
fn granted_permissions(&self) -> Vec<ToolPermission>
```

All permissions granted via `grant_session_permission()`.


**Implements:** `Agent`

### MockAgentBuilder

`autotune_mock::MockAgentBuilder`

```rust
struct MockAgentBuilder { /* fields */ }
```

Builder for [`MockAgent`].

_All fields are private._

#### Methods

```rust
fn hypothesis(self, approach: &str, hypothesis: &str, files_to_modify: &[&str]) -> Self
```

Queue a hypothesis that the research agent will return via `send()`.

```rust
fn implementation_behavior(self, behavior: ImplBehavior) -> Self
```

Set the behavior when `spawn()` is called for implementation agents.

```rust
fn implementation_script_entry(self, script: &str) -> Self
```

Queue a shell-script entry to run on the next implementer turn.
Equivalent to setting `ImplBehavior::Script` with the given entries
— provided as a builder method so tests can chain several turns.
Each entry is executed with `sh -c <entry>` in the worktree. An
empty entry is a no-op, useful to simulate an unproductive fix
turn that triggers a fresh respawn.

```rust
fn init_response(self, json: &str) -> Self
```

Queue a JSON response string for the init conversation.
The first call to `spawn()` (non-worktree) returns `init_responses[0]`;
subsequent `send()` calls cycle through the remaining responses.

```rust
fn research_response(self, raw: &str) -> Self
```

Queue a raw verbatim research-agent response (e.g. a `<plan>` or
`<request-tool>` XML fragment, or arbitrary text). Responses are
consumed in order across the research agent's `spawn()` + `send()`
calls. When any are set, they take precedence over `hypothesis()`.

```rust
fn build(self) -> MockAgent
```

Build the [`MockAgent`].

## Enums

### ImplBehavior

`autotune_mock::ImplBehavior`

```rust
enum ImplBehavior
```

Configures what the mock implementation agent does when `spawn()` is called.

#### Variants

- `CommitDummy` — Create a dummy file and commit it (makes SHA-before != SHA-after).
- `NoCommit` — Do nothing (the pipeline will record this as a crash).
- `Custom(Box<dyn Fn(&std::path::Path) + Send + Sync>)` — Run a user-provided closure with the working directory.
- `Script(Vec<String>)` — Run a shell script per implementer turn. Turns are consumed in order across `spawn()` + `send()` calls on implementer sessions, so tests can stage sequences like "first impl fails tests, fix turn repairs it" or "fresh respawn recovers after session goes unproductive". Each entry is passed to `sh -c` with cwd at the worktree; an empty entry is a no-op (useful to simulate an unproductive fix turn). Once the queue is drained, further turns are no-ops.

## Constants

### MOCK_IMPL_SESSION_PREFIX

`autotune_mock::MOCK_IMPL_SESSION_PREFIX`

```rust
const MOCK_IMPL_SESSION_PREFIX: &str = "mock-impl-";
```

Prefix applied to implementer session ids produced by the mock. The mock
relies on this string to distinguish implementer sends (fix turns) from
research sends (planning turns) since both flow through `send()`.

### MOCK_RESEARCH_SESSION_ID

`autotune_mock::MOCK_RESEARCH_SESSION_ID`

```rust
const MOCK_RESEARCH_SESSION_ID: &str = "mock-session-001";
```

Session id returned for the research agent. Kept stable so existing
scenario tests that only drive the research agent continue to work.

